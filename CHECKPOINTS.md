# wxo-js-sdk 開発チェックポイント

## 概要
このドキュメントは、開発の各段階で確認すべきチェックポイントを定義します。
各チェックポイントをクリアしてから次のフェーズに進むことで、品質を担保します。

---

## Phase 1: プロジェクト基盤構築

### Checkpoint 1.1: プロジェクト構造 ✅
**目標**: 基本的なディレクトリ構造とファイルの作成

#### 確認項目
- [ ] ディレクトリ構造が作成されている
  ```
  src/
  ├── core/
  ├── auth/
  ├── api/
  ├── chat/
  ├── agents/
  ├── ui/
  └── utils/
  ```
- [ ] `package.json` が作成されている
- [ ] `.gitignore` が作成されている
- [ ] `README.md` が更新されている

#### 成功基準
- すべてのディレクトリが存在する
- package.jsonが有効なJSON形式である
- gitignoreに必要な除外パターンが含まれている

---

### Checkpoint 1.2: 設定管理 ✅
**目標**: 外部JSONファイルからの設定読み込み機能

#### 確認項目
- [ ] `src/core/Config.js` が実装されている
- [ ] 設定の検証機能が動作する
- [ ] デフォルト値が正しく設定される
- [ ] 必須パラメータのチェックが機能する

#### テスト項目
```javascript
// 正常系
const config = new Config({
  orchestrationID: "test-id",
  hostURL: "https://example.com",
  agents: [...]
});
// → エラーなく初期化される

// 異常系
const config = new Config({});
// → 必須パラメータエラーが発生する
```

#### 成功基準
- 設定ファイルの読み込みが成功する
- バリデーションエラーが適切に表示される
- サンプル設定ファイル（`examples/config.json`）が動作する

---

### Checkpoint 1.3: HTTP通信 ✅
**目標**: 基本的なHTTP通信機能の実装

#### 確認項目
- [ ] `src/api/HttpClient.js` が実装されている
- [ ] GET/POSTリクエストが送信できる
- [ ] 認証ヘッダーが正しく設定される
- [ ] エラーハンドリングが機能する
- [ ] タイムアウト処理が実装されている

#### テスト項目
```javascript
// モックサーバーへのリクエスト
const client = new HttpClient(config);
const response = await client.get('/api/test');
// → 200 OKが返る

// エラーハンドリング
try {
  await client.get('/api/error');
} catch (error) {
  // → 適切なエラーオブジェクトが返る
}
```

#### 成功基準
- 実際のAPIエンドポイントへのリクエストが成功する
- エラー時に適切なメッセージが表示される
- ネットワークエラーが適切にハンドリングされる

---

## Phase 2: 複数エージェント対応

### Checkpoint 2.1: AgentManager実装 ✅
**目標**: 複数エージェントの管理機能

#### 確認項目
- [ ] `src/agents/AgentManager.js` が実装されている
- [ ] エージェントの追加/削除ができる
- [ ] アクティブなエージェントの切り替えができる
- [ ] 各エージェントの状態が管理される

#### テスト項目
```javascript
const manager = new AgentManager();
manager.addAgent({
  id: 'agent-1',
  name: '営業サポート',
  agentId: '...'
});
manager.selectAgent('agent-1');
// → agent-1がアクティブになる
```

#### 成功基準
- 3つ以上のエージェントを登録できる
- エージェント切り替えが正常に動作する
- 各エージェントの状態が独立している

---

### Checkpoint 2.2: エージェント選択UI ✅
**目標**: エージェント選択インターフェースの実装

#### 確認項目
- [ ] `src/ui/AgentSelector.js` が実装されている
- [ ] フローティングアイコンが表示される
- [ ] クリックでエージェント一覧が展開される
- [ ] エージェントアイコンとラベルが表示される
- [ ] アニメーション効果が動作する

#### 視覚的確認
```
1. ページ読み込み時にフローティングアイコンが表示される
2. アイコンをクリックすると複数のエージェントが展開表示される
3. 各エージェントのアイコンとラベルが正しく表示される
4. エージェントをクリックするとチャットウィンドウが開く
```

#### 成功基準
- UIが仕様通りに表示される
- クリックイベントが正常に動作する
- レスポンシブデザインが機能する

---

### Checkpoint 2.3: セッション管理 ✅
**目標**: 各エージェントの独立したセッション管理

#### 確認項目
- [ ] `src/agents/Agent.js` が実装されている
- [ ] 各エージェントが独立したWebSocket接続を持つ
- [ ] チャット履歴が個別に保存される
- [ ] エージェント切り替え時に履歴が保持される

#### テスト項目
```javascript
// エージェントAでメッセージ送信
await agentA.sendMessage('こんにちは');
// エージェントBに切り替え
manager.selectAgent('agent-b');
// エージェントAに戻る
manager.selectAgent('agent-a');
// → エージェントAの履歴が保持されている
```

#### 成功基準
- 複数エージェントの同時セッション管理が可能
- 履歴が正しく保持される
- メモリリークが発生しない

---

## Phase 3: フィードバック機能

### Checkpoint 3.1: フィードバックUI ✅
**目標**: Thumbs up/downボタンの実装

#### 確認項目
- [ ] `src/ui/FeedbackButtons.js` が実装されている
- [ ] 各メッセージにフィードバックボタンが表示される
- [ ] ボタンクリックで状態が変化する
- [ ] 選択済み状態が視覚的に表示される

#### 視覚的確認
```
メッセージ表示:
┌─────────────────────────────────┐
│ エージェント: こんにちは！      │
│ [👍] [👎]                       │
└─────────────────────────────────┘

クリック後:
┌─────────────────────────────────┐
│ エージェント: こんにちは！      │
│ [👍✓] [👎]                      │
│ フィードバックありがとうございます │
└─────────────────────────────────┘
```

#### 成功基準
- ボタンが正しく表示される
- クリックイベントが動作する
- 視覚的フィードバックが適切

---

### Checkpoint 3.2: フィードバックAPI連携 ✅
**目標**: フィードバックデータの送信

#### 確認項目
- [ ] `src/api/FeedbackAPI.js` が実装されている
- [ ] フィードバックがAPIに送信される
- [ ] 送信エラーが適切にハンドリングされる
- [ ] オプションのコメント機能が動作する

#### テスト項目
```javascript
// フィードバック送信
await feedbackAPI.send({
  messageId: 'msg-123',
  feedback: 'positive',
  comment: 'とても役立ちました'
});
// → 200 OKが返る
```

#### 成功基準
- APIへの送信が成功する
- エラー時に再試行が可能
- ユーザーに適切なフィードバックが表示される

---

## Phase 4: UI/UX改善

### Checkpoint 4.1: チャットウィンドウリサイズ ✅
**目標**: ウィンドウ幅の動的変更機能

#### 確認項目
- [ ] `src/ui/ChatWindow.js` にリサイズ機能が実装されている
- [ ] リサイズボタンが表示される
- [ ] クリックで幅が切り替わる
- [ ] アニメーションが滑らかに動作する
- [ ] サイズがlocalStorageに保存される

#### 視覚的確認
```
標準モード (400px):
┌────────────────────┐
│ 営業 [↔️] [−] [×] │
├────────────────────┤
│ チャット内容...    │
└────────────────────┘

拡大モード (600px):
┌──────────────────────────────┐
│ 営業 [↔️] [−] [×]           │
├──────────────────────────────┤
│ チャット内容...              │
└──────────────────────────────┘
```

#### 成功基準
- ボタンクリックで幅が変更される
- アニメーションが滑らかである
- 次回起動時にサイズが復元される

---

### Checkpoint 4.2: レスポンシブデザイン ✅
**目標**: モバイル・タブレット対応

#### 確認項目
- [ ] モバイル画面で適切に表示される
- [ ] タッチ操作が正常に動作する
- [ ] 画面サイズに応じてレイアウトが調整される

#### テスト環境
- [ ] デスクトップ (1920x1080)
- [ ] タブレット (768x1024)
- [ ] モバイル (375x667)

#### 成功基準
- すべての画面サイズで正常に表示される
- タッチ操作が快適である
- パフォーマンスが良好である

---

## Phase 5: 統合テスト

### Checkpoint 5.1: エンドツーエンドテスト ✅
**目標**: 全機能の統合動作確認

#### シナリオ1: 複数エージェント切り替え
```
1. ページを開く
2. フローティングアイコンをクリック
3. エージェントAを選択
4. メッセージを送信
5. エージェントBに切り替え
6. メッセージを送信
7. エージェントAに戻る
8. → エージェントAの履歴が保持されている
```

#### シナリオ2: フィードバック送信
```
1. エージェントとチャット
2. メッセージに👍をクリック
3. → フィードバックが送信される
4. → 確認メッセージが表示される
```

#### シナリオ3: ウィンドウリサイズ
```
1. チャットウィンドウを開く
2. リサイズボタンをクリック
3. → ウィンドウが拡大される
4. ページをリロード
5. → サイズが保持されている
```

#### 成功基準
- すべてのシナリオが正常に動作する
- エラーが発生しない
- ユーザー体験が良好である

---

### Checkpoint 5.2: パフォーマンステスト ✅
**目標**: パフォーマンスの確認

#### 確認項目
- [ ] 初期読み込み時間 < 2秒
- [ ] メッセージ送信レスポンス < 500ms
- [ ] エージェント切り替え < 300ms
- [ ] メモリ使用量が適切
- [ ] CPU使用率が適切

#### 測定方法
```javascript
// Chrome DevTools Performance タブで測定
// Lighthouse でスコア確認
```

#### 成功基準
- Lighthouse Performance スコア > 90
- メモリリークが発生しない
- 長時間使用でも安定している

---

### Checkpoint 5.3: セキュリティチェック ✅
**目標**: セキュリティの確認

#### 確認項目
- [ ] XSS対策が実装されている
- [ ] CSRF対策が実装されている
- [ ] 認証トークンが安全に管理されている
- [ ] 機密情報がログに出力されない
- [ ] HTTPS通信が強制されている

#### テスト項目
```javascript
// XSSテスト
sendMessage('<script>alert("XSS")</script>');
// → スクリプトが実行されない

// CSRF対策
// → CSRFトークンが正しく送信される
```

#### 成功基準
- 一般的な脆弱性が存在しない
- セキュリティベストプラクティスに準拠
- 機密情報が適切に保護されている

---

## 最終チェックポイント

### Checkpoint Final: リリース準備 ✅
**目標**: 本番環境へのリリース準備

#### ドキュメント
- [ ] README.mdが完成している
- [ ] API.mdが完成している
- [ ] GETTING_STARTED.mdが完成している
- [ ] EXAMPLES.mdが完成している
- [ ] CHANGELOGが作成されている

#### コード品質
- [ ] すべてのテストがパスする
- [ ] コードレビューが完了している
- [ ] リントエラーがない
- [ ] 未使用のコードが削除されている

#### 配布物
- [ ] dist/wxo-sdk.js が生成されている
- [ ] dist/wxo-sdk.min.js が生成されている
- [ ] package.jsonのバージョンが正しい
- [ ] LICENSEファイルが存在する

#### 動作確認
- [ ] 本番環境で動作確認済み
- [ ] 複数ブラウザで動作確認済み
  - [ ] Chrome
  - [ ] Firefox
  - [ ] Safari
  - [ ] Edge
- [ ] モバイルブラウザで動作確認済み

#### 成功基準
- すべてのチェック項目がクリアされている
- ドキュメントが完全である
- 本番環境で正常に動作する

---

## チェックポイント進捗管理

### 進捗状況
```
Phase 1: プロジェクト基盤構築
  ├─ Checkpoint 1.1: プロジェクト構造 [ ]
  ├─ Checkpoint 1.2: 設定管理 [ ]
  └─ Checkpoint 1.3: HTTP通信 [ ]

Phase 2: 複数エージェント対応
  ├─ Checkpoint 2.1: AgentManager実装 [ ]
  ├─ Checkpoint 2.2: エージェント選択UI [ ]
  └─ Checkpoint 2.3: セッション管理 [ ]

Phase 3: フィードバック機能
  ├─ Checkpoint 3.1: フィードバックUI [ ]
  └─ Checkpoint 3.2: フィードバックAPI連携 [ ]

Phase 4: UI/UX改善
  ├─ Checkpoint 4.1: チャットウィンドウリサイズ [ ]
  └─ Checkpoint 4.2: レスポンシブデザイン [ ]

Phase 5: 統合テスト
  ├─ Checkpoint 5.1: エンドツーエンドテスト [ ]
  ├─ Checkpoint 5.2: パフォーマンステスト [ ]
  └─ Checkpoint 5.3: セキュリティチェック [ ]

Final: リリース準備
  └─ Checkpoint Final: リリース準備 [ ]
```

### 更新履歴
- 2026-04-03: 初版作成